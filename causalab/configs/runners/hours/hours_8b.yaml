# @package _global_
defaults:
- /base
- /task: natural_domains_arithmetic_hours
- /model: llama31_8b
- /analysis/subspace
- _self_
task:
  target_variable: result

subspace:
  layers: [28]
  token_positions: [last_token]   # explicit single-cell position (#257)
