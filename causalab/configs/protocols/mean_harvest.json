{
  "version": "1",
  "description": "Half one of mean replacement: harvest one site over the train split and reduce it to the corpus mean AT SAVE TIME (`reduce: mean`), so the un-reduced activations never reach disk. The output is one vector, which the ablation half loads as a `params` constant. The causal protocol says to use zero and mean replacement as a matter of course and no example showed the chain, so every run re-derived it.",
  "model": {"key": "meta-llama/Llama-3.1-8B", "revision": "main"},
  "data": {
    "base": {"dataset": "weekdays/train", "field": "input"}
  },
  "positions": {"tap": {"index": -1}},
  "sites": {
    "target": {"component": "block_output", "layer": 18}
  },
  "reads": {
    "acts": {"site": "target", "pos": "tap", "model": "original", "input": "base"}
  },
  "save": [
    {"value": "acts", "model": "original", "input": "base", "reduce": "mean", "file_path": "acts.safetensors"}
  ]
}
