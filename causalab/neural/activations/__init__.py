"""Low-level activation caching and modification.

The site-grid builders live in :mod:`causalab.neural.activations.site_grids`
and are deliberately not re-exported here — import them from the module path.
"""
